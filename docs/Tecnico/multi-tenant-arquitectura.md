# Arquitectura técnica: multi-tenant (schema-por-negocio)

> **Documento vivo.** Se actualiza al cierre de cada fase si algo cambia respecto a lo aquí descrito. Ver también el documento funcional: `docs/Funcional/multi-tenant-multi-negocio.md`. Plan de implementación original: `/Users/deibymorenogarcia/.claude/plans/bueno-empieza-a-generar-soft-metcalfe.md`.

## 1. Estado de partida (auditoría)

El código es 100% single-tenant:
- Una sola conexión de BD fija por variables de entorno (`app/Config/Database.php:119-141`, lee `MYSQL_HOST_NAME`/`MYSQL_USERNAME`/`MYSQL_PASSWORD`/`MYSQL_DB_NAME` una sola vez en el constructor).
- Login sin ningún concepto de empresa (`app/Models/Employee.php::login()`, `app/Controllers/Secure_Controller.php:36-78`).
- Configuración global de una sola fila por clave para toda la instalación (`ospos_app_config`, cacheada en `app/Config/OSPOS.php:33,52,73` bajo la clave literal `'settings'`).
- `app/Config/Filters.php:110` tiene `$filters = []` vacío — no existe ningún filtro custom hoy.
- Ya existe el concepto de "sede" (`stock_locations`), pero con huecos: `sales` (cabecera), `cash_up`, `expenses`, `receivings` (cabecera) y `dinner_tables` no tienen `location_id`, a diferencia de `item_quantities`, `inventory`, `permissions`, `sales_items`.

## 2. Decisión de arquitectura

**Aislamiento por schema-por-tenant**: un único servidor MariaDB, un schema por negocio, en vez de una base de datos compartida con columna `tenant_id`.

**Por qué**: con ~40 Models existentes sin ninguna disciplina de scoping (CodeIgniter 4 no tiene "global scopes" nativos como otros frameworks), forzar `tenant_id` en cada query es alto riesgo de fuga de datos financieros entre negocios. El aislamiento físico de schema evita esa clase de bug por diseño, y requiere cambios mínimos al código de negocio ya existente y probado — la mayoría de los Models no se tocan.

**No es "un contenedor de MySQL por tenant"**: a la escala esperada (10-100 negocios), un solo servidor MariaDB con N schemas es suficiente y evita el overhead de una instancia de BD por cliente.

## 3. Topología

- **Un solo contenedor de app** (CodeIgniter) sirve a todos los tenants. Un solo deploy actualiza el código para todos simultáneamente — no hay deploy por negocio.
- **Un solo servidor MariaDB**, con un schema por tenant + un schema de control (`platform_control` o similar) para el registro de tenants y las cuentas de plataforma.
- **Traefik con una regla wildcard** (`Host(\`{tenant:[a-z0-9-]+}.midominio.com\`)`) apuntando al servicio de app único — dar de alta un negocio no toca Traefik ni la infraestructura.

## 4. Dos grupos de conexión de BD

- **`platform`**: fijo, siempre apunta al schema de control, configurado vía sus propias variables de entorno (`PLATFORM_DB_HOST_NAME`, `PLATFORM_DB_USERNAME`, `PLATFORM_DB_PASSWORD`, `PLATFORM_DB_NAME` — mismo patrón que `MYSQL_*` para los otros grupos, en `app/Config/Database.php`). Aquí viven `tenants`, `platform_accounts`, `platform_account_tenants`. BD por defecto: `platform_control`.
- **`default`**: el grupo que ya usan los ~40 Models existentes sin modificarlos (ninguno especifica `$DBGroup` explícito). Se sobreescribe dinámicamente en cada request según el tenant resuelto.

### 4.1 Las migraciones de `platform` viven en un namespace PHP separado (`Platform`, no `App`)

Las migraciones de CI4 no se filtran por `$DBGroup` al decidir cuáles corren — `php spark migrate -g platform` sin más intenta correr **todas** las migraciones de la app (las ~50 de `App\Database\Migrations`, pensadas para el schema `ospos`) contra el schema `platform_control`, porque ninguna de ellas tiene tracking previo ahí. Confirmado empíricamente: revienta a mitad de camino con errores de columnas duplicadas.

La solución correcta es un **namespace PSR-4 separado**: las 3 migraciones de esta fase viven en `app/Platform/Database/Migrations/` con namespace `Platform\Database\Migrations`, registrado en `app/Config/Autoload.php` (`'Platform' => APPPATH . 'Platform'`). Se corren con `php spark migrate -n Platform -g platform`, y `php spark migrate` (sin `-n`) sigue tocando solo `App` como siempre — namespaces distintos, cero interferencia mutua.

**Gotcha real encontrado corriendo la suite completa de tests (no solo las migraciones sueltas)**: 9 archivos de test (`EmployeeTest`, `SalesControllerTest`, `SalesKitControllerTest`, `HomeTest`, `ConfigTest`, `EmployeesControllerTest`, `CustomersCsvImportTest`, `ItemsCsvImportTest`, `Summary_taxes_test`) declaraban `protected $namespace = null;` — en CI4, `$namespace = null` en `DatabaseTestTrait` significa **"migrar todos los namespaces registrados"**, no solo `App`. Antes de esta fase eso no importaba porque `App` era el único namespace; en cuanto se registró `Platform`, esos 9 tests empezaron a intentar correr también las migraciones de `platform` (con credenciales no configuradas en el entorno de test) y fallaban con `Unable to connect to the database`. Se corrigieron los 9 archivos a `protected $namespace = 'App';` (su intención real siempre fue "solo mis migraciones"), validado con la suite completa: 166/166 verdes de nuevo. **Cualquier test nuevo que use `DatabaseTestTrait` debe declarar `$namespace` explícito (`'App'`), nunca `null`**, ahora que hay más de un namespace de migraciones en el proyecto.

## 5. Resolución de tenant (el punto técnico más delicado)

### 5.1 Por qué el orden de ejecución importa

`Config\Session::__construct()` (`app/Config/Session.php:133-149`) llama a `Database::connect()` apenas se instancia, para comprobar si existe la tabla de sesiones. La instanciación de `Config\Session` ocurre la primera vez que algo llama al helper `session()` — típicamente dentro de `Secure_Controller` (`app/Controllers/Secure_Controller.php:56`). Esto significa que **la resolución de tenant debe ocurrir antes de que cualquier cosa llame a `session()`**, o la sesión se conecta al schema equivocado.

### 5.2 Mecanismo

Un Filter nuevo de CI4 (`app/Filters/TenantResolver.php`), registrado como **primer elemento de `$required['before']`** en `app/Config/Filters.php:52-56` (no en `$globals['before']` — los filtros `required` corren antes que cualquier otro, incluso antes de que exista una ruta coincidente).

El filtro:
1. Lee el header `Host` del request.
2. Consulta `platform.tenants` (grupo de conexión `platform`, fijo — evita el problema de "necesito saber el tenant para conectarme, pero necesito conectarme para saber el tenant").
3. **Muta el array del grupo `default`** en la instancia compartida de `config('Database')` (ej. `config('Database')->default = [...]`) con el schema/usuario/contraseña del tenant resuelto, antes de que nada más llame a `db()`/`Database::connect('default')`.
4. Puebla `TenantContext` (`app/Libraries/TenantContext.php`, nuevo) con `tenant_id`, `slug` y nombre de schema — un punto único de lectura para el resto del código, en vez de esparcir parsing de `Host` por todos lados.

**Riesgo a validar con un spike aislado antes de construir nada más encima** (ver Fase 4 del plan): confirmar que ninguna otra pieza del framework abre una conexión `default` antes de que este filtro corra. Se valida con un endpoint de debug que imprime a qué schema está conectado, probado contra dos Hosts distintos.

## 6. Otros ajustes de código necesarios

### 6.1 Whitelist de hosts (`getValidHost()`)
`app/Config/App.php:319-356` hace un match **exacto** contra `allowedHostnames` (`in_array($httpHost, $this->allowedHostnames, true)`, línea 346) — bloquearía cualquier subdominio de tenant no listado literalmente. Hay que extender esta función para aceptar un patrón `*.midominio.com` con match por sufijo, sin romper el comportamiento de match exacto para hosts que no usan wildcard (`casaletto.local`, `staging.pos-casaletto.micronuba.net`, etc. seguirían funcionando igual).

### 6.2 Fuga de cache de configuración (confirmada)
`app/Config/OSPOS.php` usa la clave literal `'settings'` en `Services::cache()` (líneas 33, 52, 73). Con varios tenants compartiendo el mismo proceso PHP/cache, el negocio B leería la configuración cacheada (nombre, logo, moneda, impuestos) del negocio A. Se resuelve sufijando la clave con el slug del tenant activo, leído de `TenantContext`.

### 6.3 Defensa en profundidad a nivel de motor de BD
Cada tenant tiene su propio usuario MySQL/MariaDB con `GRANT` limitado únicamente a su propio schema, creado en el momento de provisión. Así, incluso si el Filter tuviera un bug, el motor de base de datos por sí solo rechazaría cualquier intento de cruce entre schemas — es una segunda capa de seguridad independiente del código de la aplicación.

## 7. Migraciones multi-tenant

CI4 corre `php spark migrate` contra una conexión a la vez. La tabla de tracking de migraciones (`app/Config/Migrations.php:30`, tabla `migrations` con el prefijo `ospos_` aplicado automáticamente → `ospos_migrations`) vive **dentro de cada schema**, así que el historial de cada tenant es independiente y naturalmente idempotente — no hay que inventar tracking propio.

Se necesita un **orquestador** (script bash o comando spark custom) que:
1. Recorra `platform.tenants`.
2. Corra las migraciones pendientes contra el schema de cada uno.
3. Se detenga y alerte si falla un tenant puntual, sin dejarlo a medio migrar sirviendo tráfico real.
4. Corra **antes** de promover el contenedor de app nuevo (orden: migrar todos → desplegar código).

## 8. Provisión de un tenant nuevo

Comando spark nuevo (ej. `php spark tenant:create <slug>`):
1. Crea el schema.
2. Crea el usuario MySQL con `GRANT` limitado a ese schema (sección 6.3).
3. Corre las migraciones (reusa el orquestador de la sección 7).
4. Siembra datos default (empleado admin inicial, `app_config`, sede default) reusando `app/Database/Seeds/` existente.
5. Inserta la fila correspondiente en `platform.tenants`.

### 8.1 Adopción de un negocio ya existente (Casaletto, Fase 10)

`php spark tenant:adopt <slug> <db_name_existente>` (`App\Libraries\TenantProvisioner::adopt()`) es el camino contrario: registra un schema que **ya tiene datos reales** sin crear, migrar ni resetear nada dentro de él -- el mecanismo de onboarding de Casaletto, que nunca migra sus datos a ningún lado (schema-por-tenant significa que se queda exactamente donde está).

Antes de insertar la fila en `platform.tenants`, rechaza seguir si:
- el schema no tiene las tablas `employees`/`app_config` (no parece un OSPOS real);
- su historial de migraciones del namespace `App` no está en la versión más reciente disponible (la adopción nunca migra por sí sola -- un schema atrasado debe migrarse aparte, como paso propio y revisable, antes de intentar adoptarlo);
- su cuenta admin por defecto sigue siendo la pública de upstream (`admin`/`pointofsale`, hash MD5) -- adoptarlo así expondría esa credencial en cuanto quedara alcanzable bajo el wildcard del SaaS.

A diferencia de `tenant:create`, no crea un usuario MySQL dedicado para el schema adoptado: el GRANT con wildcard de `platform_provisioner` (sección 16) solo cubre schemas `tenant_%`, y un schema preexistente con otro nombre (`ospos`) necesitaría un GRANT manual aparte, fuera del alcance de este comando automatizado. La fila queda con `db_user`/`db_password` en `NULL`, y `TenantResolver` cae al comportamiento que ya tiene ese schema hoy (credenciales compartidas) -- exactamente su comportamiento actual, sin cambios. Darle credenciales dedicadas más adelante es un endurecimiento de seguridad opcional y separado, no parte de la adopción en sí.

## 9. Infraestructura Docker/Traefik

Hoy `docker/docker-mysql.yml` es un único contenedor MariaDB (imagen `mariadb:10.5`, EOL desde jun 2025) con una sola BD creada vía `MYSQL_DATABASE`, incluido por `docker-compose.local.yml`, `docker-compose.staging.yml` y `docker-compose.prod.yml`, cada uno con un solo contenedor de app y un router Traefik **estático** (`Host()` fijo, ej. `docker-compose.prod.yml:57`).

Cambios:
- El contenedor MariaDB sigue siendo uno solo; los schemas adicionales se crean vía la herramienta de provisión (sección 8), no vía variables de entorno del contenedor.
- El router Traefik estático se reemplaza por la regla wildcard (sección 3).

## 10. Login de plataforma y plataforma de gestión de negocios

`app/Controllers/PlatformLogin.php` + `app/Models/PlatformAccount.php` (nuevos): login neutral en un subdominio propio de infraestructura (ej. `login.ospos-saas.micronuba.net`, dominio confirmado en la Fase 6 — ver sección 6.1). Si la cuenta tiene un solo tenant, entra directo; si tiene varios, selector de negocio. Al elegir, redirección al subdominio de ese negocio con **re-autenticación local** contra el `Employee::login()` existente (sin cambios) — no hay token SSO de un solo uso, se descartó por ser esfuerzo innecesario para el MVP.

**Plataforma de gestión de negocios**: panel web separado (no solo la CLI de la sección 8), accesible únicamente por `platform_accounts` con `is_platform_admin=1`, para crear, modificar, suspender y eliminar negocios-cliente. Confirmado explícitamente como entregable requerido de la Fase 8 el 2026-07-31, a raíz de que el usuario preguntó directamente si existiría un módulo para gestionar el ciclo de vida de los negocios y por dónde se entraría a él — ver el detalle de estado en la entrada de la Fase 8 más abajo. Sin capacidad de impersonation ("entrar como" un negocio-cliente), que se mantiene fuera de alcance.

## 11. Actualización de stack (incluida en el mismo trabajo)

- **MariaDB**: 10.5 (`docker/docker-mysql.yml:15`, EOL desde jun 2025) → 10.11 (LTS a feb 2028) o 11.4/11.8 (a 2028-2029).
- **PHP**: 8.2 (`Dockerfile:1`, `composer.json` `^8.2`, seguridad-solo hasta dic 2026) → 8.3 u 8.4. Bajo riesgo: la matriz de CI ya testea 8.2/8.3/8.4 (`.github/workflows/main.yml:31-33`, `phpunit.yml:37-39`).
- **CodeIgniter**: 4.7.2 → 4.7.4 no es urgente (solo 2 patches atrás).

Secuenciado **antes** de la complejidad multi-tenant, validado en staging primero, para no combinar dos cambios de riesgo en un mismo paso.

## 12. Inventario de archivos críticos

- `app/Config/Filters.php`, `app/Filters/TenantResolver.php` (nuevo), `app/Libraries/TenantContext.php` (nuevo) — resolución de tenant.
- `app/Config/Database.php` — nuevo grupo `platform`, mecánica de swap del grupo `default`.
- `app/Config/App.php` (`getValidHost()`) — soporte wildcard.
- `app/Config/OSPOS.php` (líneas 33, 52, 73) — cache key scoping.
- `app/Database/Migrations/` — migraciones de sede faltante y de las tablas de `platform`.
- `app/Database/Seeds/` — reusado por la herramienta de provisión.
- Nuevo comando spark de provisión (`app/Commands/`), nuevo script orquestador de migraciones.
- `docker/docker-mysql.yml`, `docker-compose.staging.yml`, `docker-compose.prod.yml` — regla Traefik wildcard.
- `app/Controllers/PlatformLogin.php`, `app/Models/PlatformAccount.php` (nuevos) — login de plataforma.

## 13. Verificación

- **Spike de resolución de tenant**: endpoint de debug que confirma el swap de conexión por `Host`, antes de construir el resto.
- **Staging con 2+ tenants ficticios**: aislamiento de datos, aislamiento de cache/config, `GRANT`s de BD rechazando cruce entre schemas, migraciones aplicadas correctamente a cada uno.
- **Regresión de Casaletto**: en cada fase, confirmar que el subdominio actual de Casaletto en staging/producción sigue funcionando exactamente igual (login, ventas, sedes, reportes).
- **CI existente**: mantener la matriz de PHPUnit verde en cada fase; correrla también contra la versión de PHP objetivo antes de cambiar el Dockerfile de producción.

## 14. Estrategia de rama y despliegue

Todo este trabajo se desarrolla en la rama `feature/multi-tenant-saas` (creada desde `develop`), y no se mergea a `develop` hasta que la validación en staging con tenants ficticios pase completa. Después de eso, sigue el flujo ya existente: `develop` → staging, `master` → producción.

## 15. Estado de avance

- **Fase 0 (documentación)**: completa.
- **Fase 1 (huecos de sede)**: completa **y ya desplegada en staging y producción reales** (2026-07-30). `sales`, `cash_up`, `expenses`, `receivings` y `dinner_tables` ahora tienen `location_id` (FK a `stock_locations`), con backfill de filas existentes al `MIN(location_id)` disponible. Migración: `app/Database/Migrations/20260729120000_AddLocationIdToSedeHeaders.php` + `app/Database/Migrations/sqlscripts/add_location_id_to_sede_headers.sql`. Los Models `Sale`, `Cashup`, `Expense`, `Receiving` y `Dinner_table` pueblan `location_id` en cada alta nueva reusando `Item_lib::get_item_location()` (la misma sede activa que ya usa el resto del sistema vía sesión). Validado con MariaDB 10.5 real (esquema fresco y esquema histórico de dev), y con la suite `SalesControllerTest`/`SalesKitControllerTest` (6/6 verdes) corriendo dentro del contenedor `ospos_dev` (PHP 8.2).
  - **Despliegue real (2026-07-30)**: se desacopló de la Fase 2 originalmente (cherry-pick separado), y se desplegó después vía cherry-pick a `develop`→staging y `master`→producción, con `mariadb-dump` de respaldo antes de migrar en cada ambiente (no requería el runbook de dump/restore completo de la Fase 2, al ser una migración aditiva sin salto de versión de motor). `php spark migrate` corrido vía `docker compose exec ospos` (los workflows de deploy no corren migraciones automáticamente — paso manual siempre). Verificado 100% de filas con `location_id` poblado en ambos ambientes (staging: 5 sales/2 cash_up/4 dinner_tables; producción: 335 sales/16 cash_up/20 expenses/11 dinner_tables), sin errores, HTTP 200/302 correctos.
  - **Nota**: la rama `feature/multi-tenant-saas` vivía solo local hasta este punto (nunca pusheada) — se pusheó a `origin/feature/multi-tenant-saas` como respaldo antes de este despliegue.
- **Nota de entorno de pruebas local**: `phpunit.xml.dist` fuerza `MYSQL_HOST_NAME=127.0.0.1` (pensado para el runner de GitHub Actions, donde el servicio `mysql` se expone en esa IP). Esto no coincide con `docker-compose.dev.yml`, donde MySQL vive en el hostname `mysql` de la red de Docker. Para correr PHPUnit localmente contra ese compose hace falta un puente de puerto (ej. `socat TCP-LISTEN:3306,fork,reuseaddr TCP:mysql:3306` dentro del contenedor de la app) — no se modificó `phpunit.xml.dist` porque esa configuración es correcta para CI. Vale la pena tenerlo en cuenta en fases futuras que necesiten correr la suite completa localmente.
- **Fase 2 (actualización de stack)**: completa. `Dockerfile:1` → `php:8.4-apache` (era `8.2`); `docker/docker-mysql.yml:15` y `docker-compose.dev.yml` → `mariadb:11.4` (era `10.5`, EOL desde jun 2025; se eligió 11.4 sobre 10.11 por runway más largo — LTS a mayo 2029 vs feb 2028 — dado que la plataforma se piensa para varios años de operación). También se actualizaron a las mismas versiones los workflows que hoy corren contra un stack fijo: `.github/workflows/deploy-production.yml` y `deploy-staging.yml` (el paso `docker run ... php:8.2-cli` que instala dependencias y compila assets antes del build de imagen), `.github/workflows/phpunit.yml` (contenedor MariaDB de CI) y `.github/workflows/build-release.yml` (PHP para armar el release). CodeIgniter se dejó en 4.7.2 (no es urgente, solo 2 patches atrás de 4.7.4).
  - **Validado de verdad, no solo revisado**: se reconstruyó la imagen `ospos_dev` con `php:8.4-apache` (compila limpio, incluyendo la extensión xdebug vía PECL). Se recreó el volumen local de MariaDB desde cero en 11.4 (el salto directo 10.5→11.4 sobre datos existentes falla — MariaDB exige un apagado limpio de la versión vieja primero; para este entorno de desarrollo descartable se optó por recrear en vez de migrar datos) y se corrió **el historial completo de migraciones desde el esquema inicial de 2017 hasta hoy** sin ningún error. Se corrió la suite completa de PHPUnit (166 tests / 427 assertions) dentro del contenedor con PHP 8.4, contra MariaDB 11.4: **166/166 verdes**.
  - **Nota para staging/producción**: a diferencia del entorno de dev (datos descartables), las bases de staging y producción tienen datos reales de Casaletto. El salto de MariaDB 10.5→11.4 ahí **no puede hacerse recreando el volumen** — requiere el procedimiento estándar de MariaDB (apagar limpio la versión vieja antes de arrancar la nueva sobre los mismos datos, o un dump/restore lógico) como paso explícito y cuidadoso al desplegar esta fase, no algo que ocurra solo al hacer `docker compose up`.
  - **Decisión de negocio (2026-07-29)**: el upgrade de stack en staging/producción se desacopló del resto del proyecto multi-tenant y se ejecutó de inmediato, en vez de esperar al merge completo — MariaDB 10.5 llevaba más de un año sin parches de seguridad y no tenía sentido dejarlo esperando meses.
  - **Runbook ejecutado en staging (2026-07-29), con éxito y sin pérdida de datos**:
    1. Backup en 3 capas antes de tocar nada: dump lógico (`mariadb-dump --single-transaction`) guardado en el VPS y copiado fuera de él, tarball crudo del volumen 10.5 original, y el contenedor viejo simplemente detenido (nunca borrado).
    2. App detenida para congelar escrituras antes del dump; dump verificado contra los conteos en vivo (coincidencia exacta) antes de restaurar.
    3. MariaDB 11.4 levantado en un **volumen nuevo y separado** (`pos_casaletto_staging_mysql_11_4`), dump restaurado ahí — el volumen viejo (`pos_casaletto_staging_mysql`) queda intacto como respaldo frío.
    4. **Verificación cruzada por `CHECKSUM TABLE`**: se arrancó brevemente el contenedor viejo (10.5, sin tocar) y se comparó el checksum de `sales`, `people`, `items`, `sales_items`, `employees` contra el nuevo (11.4) — **idénticos byte a byte** antes de dar el cutover por bueno.
    5. Cutover: cherry-pick del commit de stack (`docker/docker-mysql.yml`, `Dockerfile`, workflows) a `develop` — sin los documentos de este proyecto multi-tenant, que no aplican todavía a esa rama — más un commit adicional en `docker-compose.staging.yml` para que el volumen `mysql` (declarado con `driver: local` en el `docker-mysql.yml` compartido) se resuelva como un volumen `external` distinto (`mysql_11_4`) apuntando al ya migrado; Compose no permite mezclar `driver` y `external` en la misma clave, así que se usó `volumes: !override` a nivel de servicio para apuntar el mount a esa clave nueva sin arrastrar la declaración original.
    6. Deploy real en el VPS: mismo paso de build que usa `deploy-staging.yml` (composer/npm/gulp vía `php:8.4-cli`) + `docker compose up -d --build`.
    7. Smoke test: `HTTP 200` en `/login`, `HTTP 302` (redirect correcto sin sesión) en `/home`, conexión real de la app a la BD nueva confirmando los mismos datos (5 ventas) por la red de Docker, sin errores en `writable/logs`.
  - **Producción (2026-07-30), ejecutada con éxito y sin pérdida de datos**: mismo runbook exacto que staging, en la ventana sin operación confirmada por el usuario. Backup en 3 capas (dump 325 ventas / 6 empleados / 278 items — verificado contra los conteos en vivo antes de restaurar), MariaDB 11.4 restaurado en volumen nuevo (`pos_casaletto_mysql_11_4`), **checksums idénticos byte a byte** en `sales`, `people`, `items`, `sales_items`, `employees`, `cash_up`, `receivings`, `expenses` entre el 10.5 original (intacto, respaldo) y el 11.4 restaurado. Cherry-pick del cambio de stack + override de volumen a `master` (mismo patrón `volumes: !override` que en staging), deploy real vía SSH, smoke test: `HTTP 200` en `/login`, `HTTP 302` en `/home`, la app viendo las 325 ventas reales por la red de Docker, sin errores en logs. Producción corre en PHP 8.4 + MariaDB 11.4 desde este despliegue.
  - Volúmenes viejos (`pos_casaletto_staging_mysql` y `pos_casaletto_mysql`, ambos en MariaDB 10.5) y sus tarballs de respaldo se conservan sin tocar en el VPS como red de seguridad adicional — no se eliminan de inmediato.
- **Fase 3 (schema de control y registro de tenants)**: completa, solo en `feature/multi-tenant-saas` (no se despliega a staging/producción todavía — no tiene ningún uso hasta que exista el resto del multi-tenant). Grupo de conexión `platform` en `app/Config/Database.php`; 3 migraciones nuevas en `app/Platform/Database/Migrations/` (namespace `Platform`, ver sección 4.1) creando `tenants`, `platform_accounts`, `platform_account_tenants` con sus FKs. Validado de verdad: `up`/`down`/`up` corridos contra un schema `platform_control` real en Docker, esquema resultante verificado columna por columna, y la suite completa de PHPUnit corrida dos veces (detectó y permitió corregir el gotcha de `$namespace = null` en 9 tests, documentado arriba) — 166/166 verdes al cierre.
- **Fase 4 (Filter de resolución de tenant + spike de validación)**: completa, solo en `feature/multi-tenant-saas`.
  - `app/Filters/TenantResolver.php` (nuevo), registrado como primer elemento de `required.before` en `app/Config/Filters.php`. Lee el header `Host`, extrae el slug quitando el sufijo wildcard configurado, busca en `platform.tenants` (conexión `platform`, fija) un tenant `status='active'` con ese slug. Si lo encuentra, muta `config(Database::class)->default['database']` con el `db_name` del tenant y puebla `TenantContext` (`app/Libraries/TenantContext.php`, nuevo — holder estático de solo lectura). **Solo se sobreescribe `database`**, no host/usuario/contraseña — todos los tenants comparten el mismo servidor y credenciales hasta la Fase 7 (provisión), que les da un usuario MySQL propio con `GRANT` limitado a su schema.
  - Si el Host no matchea ningún wildcard configurado, o matchea pero no hay tenant activo con ese slug (incluye el caso de un tenant con `status='suspended'`), el filtro simplemente no hace nada — la conexión `default` sigue usando lo que ya configuran las variables `MYSQL_*`, es decir el comportamiento actual de Casaletto, sin cambios.
  - `app/Config/App.php`: nueva propiedad `$allowedHostnameWildcards` (sufijos con punto inicial, ej. `.midominio.com`, vía env var `ALLOWED_HOSTNAME_WILDCARDS`), consultada en `getValidHost()` además del match exacto ya existente — necesario para que `baseURL`/links generados no se rompan en subdominios de tenant.
  - `app/Config/OSPOS.php`: la clave de cache de `'settings'` ahora se sufija con el slug del tenant activo (`settings_<slug>`) vía `TenantContext::isResolved()`/`::slug()`, cerrando la fuga de config entre tenants detectada en la auditoría original.
  - **Spike de validación ejecutado de verdad** (no solo revisado): 2 schemas de tenant reales (`tenant_demo1`, `tenant_demo2`, cada uno con una tabla `marker` de valor único) + 1 tenant `suspended`, servidos por el mismo contenedor de app, probados con `curl -H "Host: ..."` contra un endpoint de debug temporal (removido antes del commit). Resultado: cada Host devolvió el `SELECT DATABASE()` y el valor de `marker` correctos de su propio schema; el tenant suspendido y un Host sin wildcard cayeron correctamente a `ospos` (el comportamiento de hoy); `/login` sin tenant siguió devolviendo `HTTP 200` sin errores. Confirma que el swap de conexión ocurre antes de que `Config\Session` (o cualquier otra cosa) abra la conexión `default`.
  - Suite completa de PHPUnit corrida después de todo esto: 166/166 verdes, sin regresión.
- **Fase 5 (orquestador de migraciones multi-tenant)**: completa, solo en `feature/multi-tenant-saas` (no wireado a `deploy-staging.yml`/`deploy-production.yml` todavía — esos ambientes no tienen schema `platform_control`, eso llega en la Fase 10).
  - `scripts/migrate-tenants.sh`: recorre `platform.tenants` (activos) y corre las migraciones de `App` contra el schema de cada uno, uno a la vez, reportando éxito/fallo por tenant y con código de salida distinto de cero si alguno falló (para que el pipeline de deploy no promueva el contenedor nuevo en ese caso).
  - `app/Commands/TenantList.php` (`php spark tenant:list`): imprime los `db_name` de tenants activos, un slug por línea, prefijados con `TENANT_DB:` — necesario porque `spark` siempre escribe su propio banner a stdout sin importar el comando, así que el script filtra por ese prefijo en vez de confiar en la salida cruda.
  - `app/Commands/TenantMigrateOne.php` (`php spark tenant:migrate-one`): migra el schema que `MYSQL_DB_NAME` indique en ese momento. **No es un wrapper del comando `migrate` de CI4** — se comprobó empíricamente que `migrate` atrapa cualquier excepción internamente y nunca la propaga como código de salida (`Boot::runCommand()` convierte un `run()` que no retorna nada en éxito) — un schema deliberadamente inalcanzable seguía reportando "OK" y saliendo con código 0. Este comando propio sí retorna 0/1 real.
  - **Segundo hallazgo real, más sutil**: el primer intento de `TenantMigrateOne` mutaba `config(Database::class)->default['database']` en tiempo de ejecución (el mismo patrón que usa `TenantResolver` en la Fase 4) — funcionó para el Filter (que corre primero que cualquier otra cosa) pero **no de forma confiable dentro de un comando spark**, donde algo ya había resuelto la conexión `default` antes de que el comando mutara la config; el síntoma fue un tenant recién creado y vacío fallando con "Duplicate column" como si ya tuviera el historial completo aplicado (de otro schema). Se resolvió abandonando la mutación en runtime y usando en su lugar la variable de entorno `MYSQL_DB_NAME` (leída una sola vez, temprano, en el constructor de `Config\Database`) — el mismo mecanismo ya probado en las Fases 1-3 — pasada por el script una vez por tenant, en un proceso `php spark` nuevo cada vez.
  - **Validado de verdad** con 3 escenarios reales en Docker: 2 tenants nuevos migrados limpio (historial completo de ~48 migraciones cada uno, incluyendo el `location_id` de la Fase 1), 1 tenant con `db_name` inexistente fallando correctamente y reportado (código de salida 1, los otros dos igual se migran, no se abortan), y el caso de cero tenants activos (código de salida 0). Re-corrida del script confirmó idempotencia (segunda vez no reaplica nada). Suite completa de PHPUnit: 166/166, sin regresión.
- **Fase 6 (infraestructura Traefik wildcard + Docker Compose)**: completa, con un rebrand de dominio y un incidente de producción durante la misma fase — detallados abajo. **Estado real a 2026-07-31: staging corre el esquema nuevo; producción corre el esquema viejo (revertido a propósito) hasta una ventana de mantenimiento explícita.**
  - Traefik real del VPS (`micronuba-traefik`, v3.3, compartido con otros proyectos) ya tiene un resolver `le-dns` (DNS-01 vía Cloudflare, credenciales ya configuradas) además del `le` (HTTP-01) que usa hoy Casaletto — confirmado que `le-dns` ya está en uso real por otro proyecto en esta misma instancia antes de usarlo aquí.
  - Sintaxis Traefik v3 (distinta de v2): `HostRegexp` ya no admite la captura con nombre `{slug:regex}` de v2 — es una regex plana. Verificado copiando la sintaxis de un router ya funcionando en este mismo Traefik: `Host(...) || HostRegexp(...)`.
  - `certresolver` de los routers de negocio pasa de `le` a `le-dns`: no es estrictamente necesario para que HTTP-01 funcione por subdominio individual, pero evita agotar los rate-limits de Let's Encrypt a medida que se agreguen tenants y no depende de que el puerto 80 esté alcanzable por cada subdominio nuevo — mismo criterio ya usado por el otro proyecto en este Traefik para su propio escenario multi-subdominio.
  - `App::$allowedHostnameWildcards` configurado vía `ALLOWED_HOSTNAME_WILDCARDS` en cada compose — reusa el mecanismo ya construido en la Fase 4.
  - **Gotcha de Docker Compose encontrado verificando (no asumido)**: `docker compose config` muestra un `$` literal duplicado como `$$` en su salida — es solo el formato de re-serialización segura de compose, **no** termina duplicado en el label real del contenedor. Verificado con un contenedor de prueba real + `docker inspect` (la fuente de verdad que Traefik lee) antes de confiar en que el anchor `$` del regex funciona.
  - **No se ha creado el registro DNS wildcard todavía** (`*.ospos-saas.micronuba.net` / `*.staging.ospos-saas.micronuba.net` → IP del VPS) — sin eso, ningún subdominio de negocio-cliente es alcanzable aún; el router nuevo es inerte para tráfico real hasta que exista, y es intencional: no rompe nada mientras tanto. Crear esos registros DNS queda para cuando haya tenants reales que provisionar (Fase 7 en adelante).

  ### 6.1 Corrección de marca del dominio SaaS (2026-07-31)

  El diseño original de la Fase 6 (ver commits `060cd758b`) reusaba el dominio propio de Casaletto como wildcard del SaaS completo: `*.pos-casaletto.micronuba.net`. El usuario identificó el problema antes de que llegara a tener tenants reales: **Casaletto es un negocio-cliente más de la plataforma, no la plataforma misma** — no se puede atar la marca de todo el SaaS a la de un solo cliente. Corrección aplicada el mismo día:
  - Dominio propio del SaaS, desacoplado de Casaletto: **`ospos-saas.micronuba.net`** (producción) / **`staging.ospos-saas.micronuba.net`** (staging), con cada negocio-cliente en `<slug>.ospos-saas.micronuba.net`. Cuando Casaletto se provisione formalmente como tenant (Fase 10), su nueva URL de plataforma será `casaletto.ospos-saas.micronuba.net` — su URL legacy (`pos-casaletto.micronuba.net`) sigue funcionando en paralelo sin cambios, no se retira.
  - **Nombre `ospos-saas` es provisional**, decidido por el usuario porque el proyecto reusa fuentes de Open Source POS: *"posteriormente lo podremos cambiar, siempre y cuando respetemos las políticas de su licenciamiento"*. No renombrar el dominio de nuevo sin antes revisar la licencia de OSPOS.
  - Al desacoplarse del dominio de Casaletto, el riesgo de colisión de un solo nivel (`staging.pos-casaletto...` matcheando el wildcard de producción, mitigado en el diseño original con `priority` 20 vs 10) ya no aplica estructuralmente — producción usa `ospos-saas.micronuba.net` y staging usa `staging.ospos-saas.micronuba.net`, dominios distintos que ninguna regex del otro puede matchear. **Los slugs reservados (`staging`, `www`, `admin`, `platform`, `login`, `api`, `app`) se mantienen** de todas formas como regla de la Fase 7 — no por colisión con el dominio de Casaletto, sino porque siguen siendo subdominios de infraestructura propios dentro de `ospos-saas.micronuba.net` (ej. un futuro `login.ospos-saas.micronuba.net` de la Fase 8).
  - Cada contenedor de app pasó a tener **dos routers Traefik simultáneos**: uno exacto para la URL legacy de Casaletto (`pos-casaletto`/`pos-casaletto-staging`, `Host()` puro) y uno nuevo para el wildcard del SaaS (`ospos-saas`/`ospos-saas-staging`, `HostRegexp()`, `priority=10`). Este cambio de "un router" a "dos routers por contenedor" fue la causa raíz del incidente descrito en 6.2.

  ### 6.2 Incidente de producción: pérdida total de servicio (2026-07-31)

  Al desplegar el rebrand de 6.1 (dos routers Traefik por contenedor en vez de uno), **tanto producción como staging dejaron de responder** — Traefik devolvía 404 plano ("no router matched") en ambos dominios legacy, sin ningún cambio funcional intencional sobre esas URLs.
  - **Causa raíz**: el proveedor Docker de Traefik solo infiere automáticamente a qué `service` pertenece un router sin label explícito **cuando el contenedor define exactamente un service**. Al agregar el segundo router (`ospos-saas`) sin agregar también `traefik.http.routers.<nombre>.service=<nombre>` explícito en **ambos** routers, Traefik no pudo resolver la ambigüedad entre los dos services del mismo contenedor y **descartó la configuración completa en silencio** — sin ninguna línea en el log de nivel `error`/`warn`. La única señal visible fue el log de acceso HTTP plano, mostrando `"-" "-"` en los campos de router/backend en vez de sus nombres reales, en lugar de un error explícito.
  - **Fix**: agregar `traefik.http.routers.<nombre>.service=<nombre>` explícito a los cuatro routers (`pos-casaletto`, `ospos-saas` en prod; `pos-casaletto-staging`, `ospos-saas-staging` en staging) — ver las líneas comentadas como *"Enlace EXPLICITO router->service"* en ambos `docker-compose.*.yml`. **Lección para cualquier contenedor futuro con más de un router Traefik**: el label `.service=` explícito no es opcional ni cosmético, es obligatorio en cuanto hay un segundo router.
  - **Corrección aplicada primero en caliente vía `scp` directo al VPS** para restaurar el servicio cuanto antes (producción estaba caída activamente), y committeada a git inmediatamente después — orden inverso al flujo normal, justificado únicamente por la urgencia de una caída real en curso.
  - **Segundo hallazgo, independiente, durante la misma investigación**: al hacer `git cherry-pick` de un commit de la rama de feature hacia `develop`, el bloque `volumes: mysql_11_4: external: true...` de `docker-compose.staging.yml` (agregado directamente en `develop`/`master` durante la Fase 2, nunca existió en `feature/multi-tenant-saas`) **desapareció silenciosamente** — el cherry-pick no reportó ningún conflicto, pero su merge de 3 vías igual descartó contenido que sí existía en la rama destino. El contenedor de staging llegó a remontar brevemente el volumen viejo (`pos_casaletto_staging_mysql`, MariaDB 10.5) en vez del migrado (`_11_4`). **Verificado con `CHECKSUM TABLE`** que ningún dato se perdió (ambos volúmenes seguían existiendo intactos; checksums de `people`/`items`/`sales_items`/`employees` idénticos; solo `sales` difería, explicado correctamente por el backfill de `location_id` de la Fase 1, posterior al snapshot). Corregido restaurando el bloque desde la copia de `master`, y sincronizando los compose de la rama de feature para que coincidan exactamente con `master` y evitar que se repita en el próximo cherry-pick.
  - **Lección operativa registrada por el usuario**: después de un cherry-pick que "auto-mergea" sin reportar conflicto, hay que diffear el archivo resultante completo contra lo que de verdad está desplegado — "sin conflicto reportado" no es sinónimo de "correcto".

  ### 6.3 Restricción operativa crítica: producción solo se toca fuera de horario

  Durante la investigación del incidente, el usuario detuvo el trabajo para aclarar una regla que rige todo este proyecto de aquí en adelante: **producción no se puede modificar mientras está operativa vendiendo en vivo — solo después de las 10pm hora Colombia**, salvo autorización explícita puntual del usuario en el momento. Esto aplica a cualquier cambio de infraestructura (Traefik, Docker Compose, deploys), no solo a este incidente puntual.
  - Como consecuencia directa, tras resolver el incidente el usuario pidió **revertir producción** al último estado bueno conocido (pre-rebrand), dejando el rollout del rebrand de dominio pendiente para una ventana de mantenimiento explícita después de las 10pm — **staging sí quedó autorizado a conservar el esquema nuevo**, por no tener tráfico real de clientes.
  - Ejecutado: `docker-compose.prod.yml` restaurado a su versión pre-rebrand (`git show be398cefa:docker-compose.prod.yml`, commit de revert `a2cbeab90` en `master`) — un solo router `pos-casaletto`, sin `ospos-saas`, `certresolver=le-dns`, `priority=10`, sin necesidad de label `.service=` explícito (un solo service, Traefik infiere solo). Redeploy vía SSH, verificado `HTTP 200`, 344 ventas sin cambio, volumen correcto (`pos_casaletto_mysql_11_4`) montado.
  - **Divergencia intencional resultante entre ramas — no es un error, no re-sincronizar sin más contexto**:
    - `master` (producción real): esquema viejo, un solo router, sin `ospos-saas`.
    - `develop` / `feature/multi-tenant-saas`: esquema nuevo, dos routers, con `ospos-saas` — vive real en **staging**, pendiente de desplegar a producción en la próxima ventana después de las 10pm hora Colombia con autorización explícita del usuario.
- **Fase 7 (herramienta de provisión de tenant)**: completa, solo en `feature/multi-tenant-saas`.
  - `app/Commands/TenantCreate.php` (`php spark tenant:create <slug> [companyName]`): crea el schema (`tenant_<slug>`), crea un usuario MySQL dedicado con `GRANT ALL` limitado a ese único schema (nada más — completa la defensa en profundidad prometida desde la Fase 4), corre las migraciones de `App` reusando `tenant:migrate-one` (Fase 5, en un proceso hijo nuevo), resetea el empleado admin por defecto, e inserta la fila en `platform.tenants` (incluyendo `db_password` cifrado con el servicio de `Encryption` de CI4).
  - **Hallazgo de seguridad real, no anticipado en el diseño original**: `initial_schema.sql` siembra el admin por defecto de cada schema nuevo con el usuario y el hash bcrypt **reales de Casaletto** (`admin_casaletto`, rotado y documentado en la memoria de branching/deploy) — sin corrección, cada tenant nuevo heredaría literalmente la contraseña de administrador de Casaletto. `tenant:create` lo corrige inmediatamente después de migrar: usuario `admin`, contraseña aleatoria nueva (impresa una sola vez en la salida del comando para entregar de forma segura), `app_config.company` actualizado al nombre del negocio.
  - Migración nueva en `platform`: `db_password` en `tenants` (Fase 3 solo tenía `db_user`, sin contraseña).
  - **`TenantResolver` (Fase 4) actualizado**: ahora también sobreescribe `username`/`password` del grupo `default` con las credenciales propias del tenant (desencriptadas), no solo `database` — cierra el hueco que la Fase 4 había dejado deliberadamente abierto ("comparten credenciales hasta la Fase 7"). Tolera con gracia una fila sin `db_user`/`db_password` (cae al comportamiento anterior, solo `database`).
  - `MYSQL_ROOT_PASSWORD` se pasa **solo para esa invocación puntual** del comando (`MYSQL_ROOT_PASSWORD=... php spark tenant:create ...`), nunca como variable de entorno permanente del contenedor de la app — el tráfico normal jamás necesita privilegios de root sobre la BD.
  - **Segundo bug real encontrado corriendo esto de verdad**: `spark` hace `chdir()` a `public/` en su propio bootstrap, así que un `exec('php spark ...')` con ruta relativa desde dentro de un comando falla ("Could not open input file: spark"). Corregido usando `ROOTPATH . 'spark'` (ruta absoluta).
  - **Validado con un tenant real de punta a punta**: schema + usuario creados, migración completa (~49 migraciones incluyendo `location_id` de la Fase 1), admin reseteado (verificado que el hash ya NO es el de Casaletto), fila en `platform.tenants` correcta. **Aislamiento de `GRANT` confirmado en dos niveles**: (1) `SHOW GRANTS` del motor confirma que el usuario del tenant solo tiene privilegios sobre su propio schema; (2) conexión real con las credenciales del tenant contra `ospos` (Casaletto) y `platform_control` — ambas **denegadas** por MySQL. Flujo HTTP completo probado con Host del tenant: `TenantResolver` resuelve y usa las credenciales dedicadas (no las compartidas), `HTTP 200` sin errores. Suite completa de PHPUnit: 166/166, sin regresión.
  - Slugs reservados (`staging`, `www`, `admin`, `platform`, `login`, `api`, `app`) y formato (1-20 caracteres, `[a-z0-9-]`) validados por el comando antes de crear nada.
- **Fase 8 (login de plataforma + plataforma de gestión de negocios)**: ✅ completa, solo en `feature/multi-tenant-saas` (no desplegada), implementada y **probada de verdad de punta a punta en Docker local** el 2026-07-31 — no solo revisada. Alcance confirmado y ampliado explícitamente con el usuario ese mismo día: *"como vamos a manejar diferentes empresas y negocios? vamos a tener una url para el logueo de cada negocio? tenemos algún módulo o plataforma para gestionar la creación, modificación o eliminación de negocios? si va a existir, como ingresamos a esa plataforma?"* — la plataforma de gestión de negocios quedó como entregable firme de esta fase, no opcional.
  - **Login neutral** (`app/Controllers/PlatformLogin.php` + `app/Models/PlatformAccount.php`, nuevos, tabla `platform.platform_accounts`, DBGroup `platform` fijo): en un subdominio propio de infraestructura (ej. `login.ospos-saas.micronuba.net`, uno de los slugs reservados de la Fase 6/7). Tras autenticar, si la cuenta es `is_platform_admin=1` redirige al panel de gestión (`platform/admin`); si no, resuelve sus tenants vía `platform_account_tenants` -- 0 tenants: error y logout; 1: redirige directo a `<slug><wildcard>`; 2+: `platform/select` (selector). La URL del negocio se arma a partir de `App::$allowedHostnameWildcards[0]` (primer sufijo configurado) -- sin wildcard configurado (dev/local), cae a `base_url()`.
  - **Plataforma de gestión de negocios** (`app/Controllers/PlatformAdmin.php`, nuevo): panel web, gateado por `is_platform_admin=1` (columna ya existente desde la Fase 3), para crear/suspender/activar/eliminar negocios-cliente sin depender de SSH/CLI al VPS. CRUD real sobre `platform.tenants`: alta (llama a `TenantProvisioner::create()`), suspensión/activación (`status`, ya soportado por `TenantResolver` desde la Fase 4), eliminación con paso de confirmación explícito (checkbox aparte para además borrar físicamente el schema -- por defecto solo revoca el usuario MySQL y borra el registro, conservando los datos como red de seguridad, mismo criterio ya usado en este proyecto para volúmenes/backups). Sin impersonation, fuera de alcance como estaba definido.
  - **`app/Libraries/TenantProvisioner.php` (nuevo)**: la lógica real de aprovisionar/suspender/eliminar un tenant, extraída de `TenantCreate.php` (Fase 7) para que la CLI y el panel web compartan el mismo código en vez de duplicarlo -- `TenantCreate.php` quedó como wrapper delgado sobre esta librería.
  - **`app/Commands/PlatformAccountCreate.php` (`php spark platform:create-account <email> [--admin]`, nuevo)**: bootstrap de cuentas de plataforma -- necesario porque no hay otra forma de crear el primer administrador de plataforma (sin el cual nadie puede entrar al panel).
  - **Cambio de decisión de seguridad, confirmado explícitamente con el usuario antes de implementar**: la CLI de la Fase 7 exigía `MYSQL_ROOT_PASSWORD` solo para esa invocación puntual, nunca variable de entorno fija del contenedor -- pero el panel web necesita poder aprovisionar en caliente desde una petición HTTP autenticada, lo que exige *alguna* credencial privilegiada disponible todo el tiempo en el contenedor. Se preguntó explícitamente al usuario cómo resolver esa tensión; eligió la opción recomendada: **un usuario MySQL dedicado y acotado (`platform_provisioner`)**, no root. Detalle de sus permisos reales (con una limitación real de MySQL/MariaDB descubierta al validar) en la sección 16.
  - **Cuatro bugs reales encontrados validando de verdad esta fase** (no solo revisando código) -- los primeros dos son **graves y preexistentes**, no introducidos por la Fase 8, pero solo se manifestaron al fin al probar un flujo HTTP completo con sesión + CSRF, algo que ninguna validación anterior (Fases 3, 4, 7) había ejercitado de punta a punta:
    1. **Toda sesión de la aplicación se destruía en cada request, en cualquier ambiente donde exista el namespace `Platform` (Fase 3 en adelante)**. `MY_Migration::get_current_version()` no filtraba por namespace, así que en cuanto se corrieron migraciones de `Platform` (números de versión más altos que las de `App`), pasó a devolver siempre la versión de `Platform` -- que la propia base de `App` nunca podía igualar -- haciendo que `Load_config::load_config()` (evento `post_controller_constructor`, se ejecuta en **cada** request) llamara `session()->destroy()` siempre. Sin este arreglo, el día que este código llegue a `develop`/`master` con la Fase 3 ya desplegada, **ningún login de Casaletto se sostendría, en ningún request**. Arreglado en dos puntos: `MY_Migration::get_current_version()` ahora filtra por `namespace` (default `'App'`), y `Load_config.php`/`Login.php` fijan explícitamente `$migration->setNamespace('App')` antes de comprobar `is_latest()`.
    2. **El propio filtro `tenantresolver` nunca corría primero, pese al comentario que lo afirmaba**. CodeIgniter, con `Config\Feature::$oldFilterOrder = false` (el valor por defecto de este proyecto), arma el antes-de-todo como `array_merge($globals['before'], $required['before'])` -- es decir, `$globals['before']` corre **antes** que `$required['before']`, al revés de lo que sugieren los nombres. `tenantresolver` estaba en `$required['before']`, así que `csrf`/`honeypot` (en `$globals['before']`) corrían primero, y CSRF dispara `session()`, que abre la conexión `default` **antes** de que `tenantresolver` la mute -- `CodeIgniter\Database\Config::connect()` cachea la conexión por nombre de grupo, así que la mutación posterior de `tenantresolver` quedaba sin efecto real para el resto del request. Confirmado empíricamente con trazas de pila reales, no supuesto. Arreglado moviendo `tenantresolver` a `$globals['before']`, como primer elemento (ver comentario largo en `app/Config/Filters.php`).
    3. **`TenantResolver` mutaba `config(Database::class)->default`, pero bajo `CI_ENVIRONMENT=development` (el que usa `docker-compose.dev.yml`) la conexión real usada es `->development`, no `->default`** (`Config\Database::$defaultGroup` cambia según el entorno). El swap era un no-op en desarrollo local. `staging`/`producción` no lo sufren porque ambos fijan `CI_ENVIRONMENT=production` (`->default` es el grupo activo ahí), pero cualquier reproducción local de un bug específico de un tenant se veía afectada. Arreglado: `TenantResolver` ahora muta `config(Database::class)->{$dbConfig->defaultGroup}`, el grupo que esté realmente activo.
    4. **La contraseña de BD de cada tenant se guardaba en base64 dos veces**, y la columna `tenants.db_password` es `VARCHAR(255)` -- el doble encoding superaba los 255 caracteres y MySQL truncaba el valor en silencio, así que `TenantResolver` fallaba a desencriptar (`EncryptionException: authentication failed`) para cualquier tenant con credenciales dedicadas (Fase 7 en adelante). `service('encrypter')->encrypt()` con `rawData=false` (la configuración de este proyecto) ya devuelve un string imprimible y seguro de guardar tal cual -- el `base64_encode()` extra en `TenantProvisioner::create()` y el `base64_decode()` extra en `TenantResolver` eran redundantes. Arreglados quitando ambos; el valor almacenado bajó de 255 (truncado) a ~192 caracteres, dentro del límite.
  - **Validado con un tenant real de punta a punta vía HTTP** (no solo la CLI): cuenta de plataforma admin creada por CLI, login vía formulario real (`curl` + cookie jar + token CSRF real extraído del HTML), creación de un negocio desde el panel web (`platform/admin/create`, sin CLI), verificado que `TenantProvisioner` corrió el mismo aprovisionamiento que la Fase 7 (admin reseteado, `app_config.company` correcto), suspensión y reactivación desde el panel, y **el negocio nuevo resuelto correctamente por Host** (`Host: demo1.<wildcard>` → login mostrando el nombre del negocio nuevo, no el de Casaletto/default) confirmando que el swap de conexión (con los 3 primeros arreglos de arriba) funciona de verdad para un flujo HTTP completo, no solo un endpoint de debug aislado como en la Fase 4.
  - **Fuera de alcance de la Fase 8** (sin cambios): SSO real de un solo token, impersonation de soporte, analítica cross-negocio.
  - **Nota aparte, no de la Fase 8**: `vendor/bin/phpunit` corrido a mano vía `docker compose exec` en local (no el workflow de GitHub Actions) falla en 129/131 tests con `Undefined array key 0` en `App::getValidHost()`, por `ALLOWED_HOSTNAMES` no configurado para ese proceso puntual. **Confirmado con `git stash` que este fallo ya existía en el código sin tocar de esta rama** -- no es una regresión de la Fase 8, es una brecha del entorno de prueba ad-hoc local (el workflow de CI probablemente fija esa variable a nivel de job). No se investigó más a fondo por estar fuera de alcance; queda anotado para no re-diagnosticarlo en una sesión futura.
- **Fase 9 (validación en staging con tenants ficticios + adopción real de Casaletto)**: ✅ completa, ejecutada el 2026-07-31. Primer despliegue real de `feature/multi-tenant-saas` a un ambiente vivo -- **desplegada directo al VPS de staging por SSH, sin mergear a `develop` primero**, decisión explícita del usuario para no dejar que `develop` (a un solo comando de promoverse a `master`) cargue código que altera el comportamiento de cada request antes de validarlo. `docker-compose.staging.yml` (y `docker-compose.prod.yml`, sin desplegar aún) se actualizaron para pasar `PLATFORM_DB_*`/`PLATFORM_PROVISION_*` al contenedor.
  - **Runbook ejecutado en staging, con 3 capas de respaldo primero** (mismo criterio que la migración de MariaDB de la Fase 2): dump lógico (`mariadb-dump --single-transaction`, 197KB) copiado también fuera del VPS, más un tarball del volumen crudo -- ninguno de los dos hizo falta, pero quedaron ahí antes de tocar nada, no después.
  - **Hallazgo real: el usuario compartido (`MYSQL_USERNAME`) NO tiene privilegios automáticos sobre un schema creado después del arranque del contenedor.** La imagen oficial de MariaDB solo otorga privilegios sobre la BD declarada en `MYSQL_DATABASE` al iniciar; `platform_control` es un schema nuevo, así que hubo que agregar un `GRANT ALL PRIVILEGES ON platform_control.* TO '<MYSQL_USERNAME>'@'%'` explícito como paso manual de una sola vez -- no lo hace ninguna migración, no está automatizado, y hay que repetirlo en producción cuando llegue esa fase.
  - **Segundo hallazgo, evitable la próxima vez**: se desplegó el código a staging antes de haber hecho `git commit`+`push` de `TenantAdopt.php` y de `TenantProvisioner::adopt()` -- el primer intento de `tenant:adopt` en staging falló con "Command not found" porque el checkout remoto no tenía esos archivos todavía. Corregido commiteando y pusheando, luego `git fetch`+`reset --hard` de nuevo en el VPS y reconstruyendo. **Lección: confirmar `git status`/`git log` local esté limpio y pusheado ANTES de desplegar a cualquier ambiente remoto**, no asumir que "lo escribí" significa "está en el commit que se va a desplegar".
  - El resto del runbook de la sección 16 (crear `platform_provisioner` con sus GRANTs) funcionó **correctamente al primer intento** en staging, sin los tropiezos de escritura de wildcard escapado que sí ocurrieron ensayando esto en local por primera vez -- la validación local sirvió exactamente para lo que se hizo: encontrar los tropiezos en un ambiente descartable antes de repetirlos en uno real.
  - **Smoke test antes y después de cada paso, no solo al final**: tras el `docker compose up -d --build` inicial (que ya traía todos mis arreglos de bugs de sesión/orden de filtros corriendo por primera vez fuera de mi entorno local), se confirmó `HTTP 200` en la URL legacy de Casaletto en staging ANTES de seguir con el resto del runbook -- si algo se hubiera roto ahí, se habría detectado de inmediato, no al final de una cadena larga de pasos.
  - **Adopción real de Casaletto ejecutada y verificada exhaustivamente**: `php spark tenant:adopt casaletto ospos` contra el schema real de Casaletto en staging. Verificado con consultas directas, antes y después: conteos de `employees`/`sales`/`items`/`people` **idénticos byte a byte** al baseline tomado antes de tocar nada; el admin (`admin_casaletto`) sin modificar; **ningún schema `tenant_%` creado** (confirma que `adopt()` de verdad no provisiona nada nuevo); la fila en `platform.tenants` con `db_user`/`db_password` en `NULL` (usa las credenciales compartidas que Casaletto ya usa hoy, tal como está diseñado). Resuelto por Host (`Host: casaletto.staging.ospos-saas.micronuba.net`, sin necesitar el registro DNS wildcard todavía -- probado con el header directo) devolvió una página **idéntica** a la de la URL legacy (mismo título, mismo tamaño de respuesta, misma configuración). La URL legacy de Casaletto en staging siguió funcionando exactamente igual durante y después de todo el proceso.
  - **Plataforma de gestión de negocios probada en un ambiente real, no solo local**: login del administrador de plataforma vía HTTPS real (`https://staging.pos-casaletto.micronuba.net/platform/login`, con certificado válido), panel cargado, Casaletto visible en el listado de negocios.
  - **Tercer hallazgo real, encontrado por el usuario comparando visualmente staging contra producción (no por ningún smoke test automático)**: la UI de staging se veía completamente rota -- iconos desordenados, sin estilos, contenido duplicado. Causa: al reconstruir la imagen por segunda vez (para recoger `TenantAdopt.php` recién pusheado) se hizo `git fetch` + `git reset --hard`, que resetea `header.php`/`login.php` a su estado SIN compilar, y se pasó directo a `docker compose up --build` sin volver a correr el build de assets (composer/npm/gulp) -- exactamente la recurrencia del [[gulp-inject-build-gotcha]] ya documentado: `header.php` quedó con **0 referencias** a `resources/css`/`resources/js` (contra 54 en producción), la página cargaba `HTTP 200` igual (por eso ningún smoke test lo detectó) pero sin una sola hoja de estilo ni script. **Lección reforzada: cualquier `docker compose up --build` manual debe ir precedido del build completo de assets SIEMPRE, incluso en un "solo reconstruyo la imagen" de seguimiento** -- un `git reset --hard` deshace en silencio lo que gulp había inyectado. Corregido re-corriendo el build, confirmando las 54 referencias en el host antes de reconstruir, y verificado visualmente después.
  - **Pendiente, deliberadamente fuera de esta fase**: crear el registro DNS wildcard real (`*.staging.ospos-saas.micronuba.net`) -- todo lo anterior se validó con `curl -H "Host: ..."`, que no lo necesita; provisionar 2 tenants ficticios adicionales con datos distinguibles para la validación de aislamiento cruzado que el plan original describía (el ensayo de adopción de Casaletto tomó prioridad porque es lo que de verdad bloquea Fase 10); GRANT dedicado (usuario MySQL propio) para el Casaletto adoptado -- ver Fase 10 más abajo; también quedó pendiente igualar `company`/`theme` de staging a los de producción (`Open Source Point of Sale`/`darkly` vs `Casaletto Anapoima`/`flatly`) -- diferencia de datos preexistente, no relacionada con este proyecto, el usuario decidirá si vale la pena igualarla.
- **Fase 10 (rollout a producción)**: pendiente. La adopción de Casaletto en producción sigue exactamente este mismo runbook (ahora ensayado y probado en staging), en una ventana explícita después de las 10pm hora Colombia con autorización puntual del usuario -- no antes. Ver `app/Commands/TenantAdopt.php` y `App\Libraries\TenantProvisioner::adopt()`.

## 16. Runbook: usuario `platform_provisioner` (una sola vez por ambiente, manual, NO automatizado)

El panel web de la Fase 8 necesita crear/eliminar schemas y usuarios MySQL en caliente desde una petición HTTP. En vez de usar `root` (la opción más simple mencionada y descartada), el usuario eligió explícitamente un usuario MySQL dedicado y acotado, `platform_provisioner`, como variable de entorno fija del contenedor de app (`PLATFORM_PROVISION_USERNAME`/`PLATFORM_PROVISION_PASSWORD`) -- a diferencia de `MYSQL_ROOT_PASSWORD` en la Fase 7, que nunca era una variable de entorno permanente.

**Limitación real de MySQL/MariaDB descubierta al validar esto de verdad (no asumida)**: un GRANT con wildcard sobre un patrón (`` `tenant_%`.* ``) SÍ funciona para operar dentro de schemas ya existentes que matcheen el patrón (`INSERT`/`SELECT`/`GRANT` posterior a un tenant específico), pero **NO autoriza el propio `CREATE DATABASE`** para una base que todavía no existe -- confirmado con un `Access denied` real al intentarlo, primero con el patrón mal escapado (`` `tenant\_%` ``, que además resultó ser un error de sintaxis de escape en el propio GRANT) y después incluso con el patrón correcto sin escapar. MySQL/MariaDB exige el privilegio `CREATE`/`DROP` **global** (`ON *.*`) para poder ejecutar `CREATE DATABASE`/`DROP DATABASE`, sin importar cómo esté escrito el patrón del GRANT de base de datos. Esto es una limitación real del motor, no un error de sintaxis a corregir.

**Implicación de seguridad, para que quede explícita y no sobre-vendida**: `platform_provisioner` puede crear/eliminar *cualquier* base de datos del servidor (no solo `tenant_*`) y crear/eliminar *cualquier* usuario MySQL -- eso es inherente a como MySQL modela estos privilegios, no se puede acotar más por patrón de nombre. Lo que sí queda estrictamente acotado: `platform_provisioner` **no tiene ningún privilegio de datos** (`SELECT`/`INSERT`/`UPDATE`/`DELETE`) sobre `ospos` (Casaletto), `platform_control`, ni ningún otro schema existente que no matchee `tenant_%` -- solo puede *operar* dentro de un schema `tenant_*` una vez creado, vía el GRANT con wildcard. Sigue siendo una reducción real de superficie de ataque frente a usar `root` (sin `SUPER`, sin `RELOAD`, sin acceso a datos reales de ningún negocio), solo que la frontera de lo que protege es "no puede leer/escribir datos de negocios", no "no puede tocar nada fuera de tenant_*".

SQL a ejecutar una sola vez por ambiente (local, staging, producción -- cuando cada uno reciba esta fase), conectado como root:

```sql
CREATE USER 'platform_provisioner'@'%' IDENTIFIED BY '<contraseña fuerte, generada, guardada solo en el .env del VPS>';
GRANT CREATE, DROP ON *.* TO 'platform_provisioner'@'%';
GRANT CREATE USER ON *.* TO 'platform_provisioner'@'%';
GRANT ALL PRIVILEGES ON `tenant_%`.* TO 'platform_provisioner'@'%' WITH GRANT OPTION;
```

Nota: **no hace falta `FLUSH PRIVILEGES`** después de `GRANT`/`CREATE USER`/`DROP USER` -- esas sentencias ya toman efecto de inmediato; `TenantProvisioner` deliberadamente no lo ejecuta, porque `FLUSH PRIVILEGES` requiere el privilegio `RELOAD`, que este usuario no tiene a propósito (confirmado con un `Access denied; you need (at least one of) the RELOAD privilege(s)` real al probarlo).

Variables de entorno nuevas a agregar al `.env` de cada ambiente (staging/producción, cuando se despliegue esta fase) y a los `docker-compose.*.yml` correspondientes (siguiendo el patrón `${VAR}` ya usado para `MYSQL_*`/`ENCRYPTION_KEY`, nunca hardcoded):
```
PLATFORM_PROVISION_USERNAME=platform_provisioner
PLATFORM_PROVISION_PASSWORD=...
```
